---
# ---------------------------------------------------------------------------
# Receiver resolution
# ---------------------------------------------------------------------------

- name: Determine Splunk receiver host
  ansible.builtin.set_fact:
    splunk_receiver_effective_host: >-
      {{
        (splunk_receiver_host | default('') | trim)
        if (splunk_receiver_host | default('') | trim) != ''
        else (
          (hostvars['splunk']['ansible_host']
            | default(hostvars['splunk']['inventory_hostname'])
            | default(''))
          if (splunk_receiver_from_inventory | default(true) | bool and 'splunk' in hostvars)
          else ''
        )
      }}
    splunk_receiver_effective_port: "{{ splunk_receiver_port | default(9997) }}"
  changed_when: false

- name: Fail if Splunk receiver host is not defined
  ansible.builtin.fail:
    msg: >-
      Splunk receiver host not set. Set splunk_receiver_host (preferred) in group vars,
      OR ensure inventory contains a host named 'splunk' with ansible_host.
  when: splunk_receiver_effective_host | trim == ""

# ---------------------------------------------------------------------------
# UF presence check (idempotent fast path)
# ---------------------------------------------------------------------------

- name: Check if UF is already installed
  ansible.builtin.stat:
    path: "{{ splunk_universal_universal_forwarder_bin }}"
  register: universal_universal_forwarder_installed
  become: true

# ---------------------------------------------------------------------------
# Package selection / source path
# ---------------------------------------------------------------------------

- name: Detect package manager (deb vs rpm)
  ansible.builtin.set_fact:
    _universal_universal_forwarder_pkg_type: "{{ 'rpm' if (ansible_pkg_mgr | default('') | lower) in ['dnf','yum','zypper','rpm'] else 'deb' }}"
  changed_when: false

- name: Select UF download URL for platform
  ansible.builtin.set_fact:
    splunk_universal_forwarder_url: "{{ splunk_universal_forwarder_rpm_url if _universal_universal_forwarder_pkg_type == 'rpm' else splunk_universal_forwarder_deb_url }}"
    splunk_universal_forwarder_pkg: "{{ (splunk_universal_forwarder_rpm_url if _universal_universal_forwarder_pkg_type == 'rpm' else splunk_universal_forwarder_deb_url) | basename }}"
  changed_when: false

- name: Set UF package path (local override supported)
  ansible.builtin.set_fact:
    splunk_universal_forwarder_pkg_path: >-
      {{
        (splunk_universal_forwarder_local_pkg | default('') | trim)
        if (splunk_universal_forwarder_local_pkg | default('') | trim) != ''
        else "/tmp/{{ splunk_universal_forwarder_pkg }}"
      }}
  when: not universal_universal_forwarder_installed.stat.exists
  changed_when: false

# ---------------------------------------------------------------------------
# Obtain package (download only if needed)
# ---------------------------------------------------------------------------

- name: Download UF package when no local package is provided
  ansible.builtin.get_url:
    url: "{{ splunk_universal_forwarder_url }}"
    dest: "/tmp/{{ splunk_universal_forwarder_pkg }}"
    mode: "0644"
    validate_certs: "{{ splunk_universal_forwarder_validate_certs | default(true) | bool }}"
    timeout: 300
  when:
    - not universal_universal_forwarder_installed.stat.exists
    - (splunk_universal_forwarder_local_pkg | default('') | trim) == ''
  become: true

- name: Verify UF package exists
  ansible.builtin.stat:
    path: "{{ splunk_universal_forwarder_pkg_path }}"
  register: universal_universal_forwarder_pkg_stat
  when: not universal_universal_forwarder_installed.stat.exists
  become: true

- name: Fail if UF package is missing
  ansible.builtin.fail:
    msg: "UF package not found at {{ splunk_universal_forwarder_pkg_path }}."
  when:
    - not universal_universal_forwarder_installed.stat.exists
    - not universal_universal_forwarder_pkg_stat.stat.exists

# ---------------------------------------------------------------------------
# Install UF
# ---------------------------------------------------------------------------

- name: Install UF (Debian/Ubuntu)
  ansible.builtin.apt:
    deb: "{{ splunk_universal_forwarder_pkg_path }}"
    state: present
  when:
    - not universal_universal_forwarder_installed.stat.exists
    - _universal_universal_forwarder_pkg_type == 'deb'
  become: true

- name: Install UF (RedHat/Fedora)
  ansible.builtin.dnf:
    name: "{{ splunk_universal_forwarder_pkg_path }}"
    state: present
    disable_gpg_check: true
  when:
    - not universal_universal_forwarder_installed.stat.exists
    - _universal_universal_forwarder_pkg_type == 'rpm'
  become: true

# Re-check UF presence after install
- name: Re-check UF binary exists
  ansible.builtin.stat:
    path: "{{ splunk_universal_universal_forwarder_bin }}"
  register: universal_universal_forwarder_bin_after
  become: true

- name: Fail if UF binary missing after install
  ansible.builtin.fail:
    msg: "UF binary not found at {{ splunk_universal_universal_forwarder_bin }} after install."
  when: not universal_universal_forwarder_bin_after.stat.exists

# ---------------------------------------------------------------------------
# First start / license (idempotent)
# ---------------------------------------------------------------------------

- name: Start UF and accept license (first run only)
  ansible.builtin.command:
    cmd: "{{ splunk_universal_universal_forwarder_bin }} start --accept-license --answer-yes --no-prompt"
  args:
    creates: "{{ splunk_universal_universal_forwarder_home }}/var/log/splunk/splunkd.log"
  become: true
  become_user: "{{ splunk_universal_universal_forwarder_user }}"
  changed_when: true
  failed_when: false

# ---------------------------------------------------------------------------
# Persist across reboot (best effort)
# ---------------------------------------------------------------------------

- name: Enable UF boot-start (best effort)
  ansible.builtin.command:
    cmd: "{{ splunk_universal_universal_forwarder_bin }} enable boot-start -user {{ splunk_universal_universal_forwarder_user }} --accept-license --answer-yes --no-prompt"
  register: universal_universal_forwarder_bootstart
  changed_when: "'configured to start automatically' in (universal_universal_forwarder_bootstart.stdout | lower | default(''))"
  failed_when: false
  become: true

# ---------------------------------------------------------------------------
# Configure outputs.conf / inputs.conf
# ---------------------------------------------------------------------------

- name: Ensure UF local config directory exists
  ansible.builtin.file:
    path: "{{ splunk_universal_universal_forwarder_home }}/etc/system/local"
    state: directory
    owner: "{{ splunk_universal_universal_forwarder_user }}"
    group: "{{ splunk_universal_universal_forwarder_user }}"
    mode: "0750"
  become: true

- name: Configure outputs.conf default group
  ansible.builtin.ini_file:
    path: "{{ splunk_universal_universal_forwarder_home }}/etc/system/local/outputs.conf"
    create: true
    section: tcpout
    option: defaultGroup
    value: "splunk_indexers"
    owner: "{{ splunk_universal_universal_forwarder_user }}"
    group: "{{ splunk_universal_universal_forwarder_user }}"
    mode: "0600"
  become: true
  notify:
    - restart splunk universal_forwarder
    - wait for universal_universal_forwarder mgmt port

- name: Configure outputs.conf receiver target
  ansible.builtin.ini_file:
    path: "{{ splunk_universal_universal_forwarder_home }}/etc/system/local/outputs.conf"
    create: true
    section: tcpout:splunk_indexers
    option: server
    value: "{{ splunk_receiver_effective_host }}:{{ splunk_receiver_effective_port }}"
    owner: "{{ splunk_universal_universal_forwarder_user }}"
    group: "{{ splunk_universal_universal_forwarder_user }}"
    mode: "0600"
  become: true
  notify:
    - restart splunk universal_forwarder
    - wait for universal_universal_forwarder mgmt port

# -------------------------
# Inputs: audit (only if file exists)
# -------------------------

- name: Check audit log exists
  ansible.builtin.stat:
    path: /var/log/audit/audit.log
  register: audit_log
  become: true

- name: Configure audit log input (only if present)
  ansible.builtin.blockinfile:
    path: "{{ splunk_universal_universal_forwarder_home }}/etc/system/local/inputs.conf"
    create: true
    owner: "{{ splunk_universal_universal_forwarder_user }}"
    group: "{{ splunk_universal_universal_forwarder_user }}"
    mode: "0600"
    marker: "# {mark} ANSIBLE CCDC AUDIT INPUT"
    block: |
      [monitor:///var/log/audit/audit.log]
      disabled = 0
      index = {{ splunk_universal_universal_forwarder_index }}
      sourcetype = linux_audit
  when:
    - splunk_universal_universal_forwarder_enable_audit | bool
    - audit_log.stat.exists
  become: true
  notify:
    - restart splunk universal_forwarder
    - wait for universal_universal_forwarder mgmt port

# -------------------------
# Inputs: auth/secure (only if file exists)
# -------------------------

- name: Check Debian auth log exists
  ansible.builtin.stat:
    path: /var/log/auth.log
  register: deb_auth_log
  when: ansible_os_family == "Debian"
  become: true

- name: Check RedHat secure log exists
  ansible.builtin.stat:
    path: /var/log/secure
  register: rh_secure_log
  when: ansible_os_family == "RedHat"
  become: true

- name: Configure auth input (Debian, only if present)
  ansible.builtin.blockinfile:
    path: "{{ splunk_universal_universal_forwarder_home }}/etc/system/local/inputs.conf"
    create: true
    owner: "{{ splunk_universal_universal_forwarder_user }}"
    group: "{{ splunk_universal_universal_forwarder_user }}"
    mode: "0600"
    marker: "# {mark} ANSIBLE CCDC AUTH INPUT"
    block: |
      [monitor:///var/log/auth.log]
      disabled = 0
      index = {{ splunk_universal_universal_forwarder_index }}
      sourcetype = linux_secure
  when:
    - splunk_universal_universal_forwarder_enable_auth | bool
    - ansible_os_family == "Debian"
    - deb_auth_log.stat.exists
  become: true
  notify:
    - restart splunk universal_forwarder
    - wait for universal_universal_forwarder mgmt port

- name: Configure auth input (RedHat, only if present)
  ansible.builtin.blockinfile:
    path: "{{ splunk_universal_universal_forwarder_home }}/etc/system/local/inputs.conf"
    create: true
    owner: "{{ splunk_universal_universal_forwarder_user }}"
    group: "{{ splunk_universal_universal_forwarder_user }}"
    mode: "0600"
    marker: "# {mark} ANSIBLE CCDC AUTH INPUT"
    block: |
      [monitor:///var/log/secure]
      disabled = 0
      index = {{ splunk_universal_universal_forwarder_index }}
      sourcetype = linux_secure
  when:
    - splunk_universal_universal_forwarder_enable_auth | bool
    - ansible_os_family == "RedHat"
    - rh_secure_log.stat.exists
  become: true
  notify:
    - restart splunk universal_forwarder
    - wait for universal_universal_forwarder mgmt port

# ---------------------------------------------------------------------------
# Validation (proves forwarding is configured, not just installed)
# ---------------------------------------------------------------------------

- name: UF status (non-fatal, informational)
  ansible.builtin.command:
    cmd: "{{ splunk_universal_universal_forwarder_bin }} status"
  become: true
  become_user: "{{ splunk_universal_universal_forwarder_user }}"
  register: universal_universal_forwarder_status
  changed_when: false
  failed_when: false

- name: List forward servers
  ansible.builtin.command:
    cmd: "{{ splunk_universal_universal_forwarder_bin }} list forward-server"
  become: true
  become_user: "{{ splunk_universal_universal_forwarder_user }}"
  register: universal_universal_forwarder_fwd
  changed_when: false
  failed_when: false

- name: Fail if receiver not present in forwarding list
  ansible.builtin.fail:
    msg: |
      UF installed but forwarding not confirmed.
      Expected receiver: {{ splunk_receiver_effective_host }}:{{ splunk_receiver_effective_port }}

      UF status:
      {{ universal_universal_forwarder_status.stdout | default('') }}

      Forward-server output:
      {{ universal_universal_forwarder_fwd.stdout | default('') }}

      Common causes:
      - Receiver IP/host wrong
      - Network path blocked
      - UF not restarted after config change
  when: (splunk_receiver_effective_host ~ ":" ~ splunk_receiver_effective_port|string) not in (universal_universal_forwarder_fwd.stdout | default(''))
