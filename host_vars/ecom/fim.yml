---
# Host-aware FIM scope for the Ubuntu ecom web server

fim_aide_paths:
  - /etc
  - /var/www

# Exclude volatile locations (noise)
fim_aide_excludes:
  - /var/log
  - /var/run
  - /run
  - /tmp
  - /var/tmp
  - /var/cache
  - /var/lib/aide
  - /var/spool
