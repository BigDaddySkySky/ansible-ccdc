---
- name: Hello World - SSH Availability Check
  hosts: all
  gather_facts: false

  tasks:
    - name: Verify SSH availability for competition operations
      ansible.builtin.ping:
      register: ping_result

    - name: Display result
      ansible.builtin.debug:
        msg: "✅ {{ inventory_hostname }} is reachable!"
      when: ping_result is success

- name: Summary
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Show summary
      ansible.builtin.debug:
        msg:
          - "╔══════════════════════════════════════════╗"
          - "║   SSH AVAILABILITY CONFIRMED             ║"
          - "╚══════════════════════════════════════════╝"
          - ""
          - "✅ All hosts reachable via SSH!"
