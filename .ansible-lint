warn_list:
  - fqcn-builtins # allow short module names for speed under competition

skip_list:
  - experimental # avoid nonessential checks
  - no-changed-when # many commands are intentional
  - var-naming # allow existing vars
  - yaml[document-start]

enable_list:
  - yaml
